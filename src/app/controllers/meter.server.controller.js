const _ = require('lodash')
const mongoose = require('mongoose')
const User = mongoose.model('Meter')
const helper = require('../libs/helper')
const { handleError, handleSuccess } = require('../../config/response')

exports.create = async (req, res) => {
  try {
    
  } catch (error) {

  }
}

